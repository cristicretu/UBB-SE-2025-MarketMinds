<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MarketMinds.SeeBuyerReviewsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MarketMinds"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SeeBuyerReviewsView">

    <Grid>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <!-- User and rating -->
                <RowDefinition Height="*" />
                <!-- Listings -->
                <RowDefinition Height="Auto" />
                <!-- Pagination -->
            </Grid.RowDefinitions>
            <ListView x:Name="ReviewsListView" Grid.Row="2" ItemsSource="{x:Bind ViewModel.reviews, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="1" BorderBrush="Gray" Padding="10" Margin="5">
                            <StackPanel>
                                <TextBlock Text="{Binding buyerId}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Margin="0,5,0,5"/>
                                <TextBlock Text="Price: $" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                <RatingControl  x:Name="RatingStars" Value="{Binding Rating, Mode=OneWay}" 
                                                IsClearEnabled="True" IsReadOnly="False" MaxRating="5" 
                                                PlaceholderValue="0.5" Foreground="Gold" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!-- This TextBlock will be shown when there are no reviews -->
            <TextBlock x:Name="EmptyMessageTextBlock"
                   Text="No results found."
                   Visibility="Collapsed"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Foreground="Gray"/>      
        </Grid>
    </Grid>
</Window>
